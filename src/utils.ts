import { Triplet } from "@react-three/cannon";

const START_CAR_POSITION: Triplet = [6, 1, 7];
const START_CAR_ROTATION_Y = (65 * Math.PI) / 180;

export { START_CAR_POSITION, START_CAR_ROTATION_Y };
